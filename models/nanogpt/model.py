"""PLACEHOLDER -- verbatim copy of karpathy/nanoGPT ``model.py`` goes here.

Do not write a reimplementation.  Copy the upstream file at a pinned commit and
record the hash in ``UPSTREAM.txt``.  The point of using nanoGPT as the sandbox
is that it is a known-correct, widely-read GPT with weight tying, so a gate
failure is unambiguously the engine's fault.

The only expected local change is the ability to disable weight tying, because
gates 5a-5d run untied (tying is gate 5e's problem) and the untied engine is
the diff target when 5e fails.
"""

raise NotImplementedError("copy nanoGPT model.py at a pinned commit; see UPSTREAM.txt")
